import { View, Text } from 'react-native';
import React from 'react';
import styles from 'common/styles';

const SwapsScreen = () => {
  return (
    <View style={styles.flexCenter}>
      <Text>Swaps Screen</Text>
    </View>
  );
};

export default SwapsScreen;
